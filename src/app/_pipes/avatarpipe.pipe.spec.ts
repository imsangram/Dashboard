import { AvatarpipePipe } from './avatarpipe.pipe';

describe('AvatarpipePipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarpipePipe();
    expect(pipe).toBeTruthy();
  });
});
